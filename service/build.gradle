repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework:spring-context:5.1.14.RELEASE'
}

test {
    useJUnitPlatform()
    testLogging {
        events = ["passed", "failed", "skipped"]
    }
}